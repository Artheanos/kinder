export type EventBasicObject = {
    title: string,
    address: string,
    lat: number,
    lng: number,
    description: string,
    startDate: Date,
    endDate: Date,
    capacity: string,
    state: string,
}